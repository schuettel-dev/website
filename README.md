# schuettel.dev

## Run locally

```shell
./development.sh
```

## Build

```shell
./build.sh
```

It copies `./src` to `./_site`.

## Build and preview

```shell
netlify dev
```

* Requires Netlify CLI: `npm install netlify-cli -g`
* Does not watch changes
